# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.0] - 2026-09-08

### ⚠️ 升级注意

- 配置结构重构为分组嵌套（作业检测 / 点名检测 / 免打扰时段），**旧版本配置将重置为默认值**，请升级后到配置面板重新设置。

### 新增

- 配置面板按功能分 3 组展示（`homework_check` / `rollcall_check` / `quiet_hours`）。
- 作业检测总开关 `enable_homework_check`：关闭后不注册定时任务、不发包检测、不推送作业通知；不影响 `/更新作业` 手动命令。
- 点名检测总开关 `enable_rollcall_check`：关闭后不注册定时任务、不发包检测、不推送点名通知。
- 课内点名检测间隔 `rollcall_in_class_interval`（默认 1 分钟）：有课表时上课时间内的检测频率可配置。
- `/我的状态` 新增作业/点名两个总开关状态展示。

### 变更

- 课内点名检测由"每分钟固定发包"改为按课内间隔节流（默认 1 分钟，行为与旧版一致）；课内/无课表两种模式共用同一时间戳节流。

### 移除

- README 开发章节（pytest 说明与分层描述）。

## [v1.0.0] - 2026-09-07

首个正式发布版本。

### 新增

- 微信扫码登录与账号密码登录（CAS SSO + MFA 短信二次认证），登录频率限制与登录流程命令拦截。
- 作业查询（`/作业列表` 离线缓存、`/更新作业` 手动刷新）。
- 作业自动提醒：新作业发布通知 + 快到期分级去重提醒（24h/6h/1h 各推一次）。
- 课表驱动点名通知：上传 .ics 课表后仅在上课时间段内检测（含课前提前窗口）；课表过期自动回退定时轮询并提醒。
- 免打扰时段：时段内暂停定时推送与检测，结束后自动补推。
- `/登出` 自助解绑（二次确认）、`/我的状态` 自查（登录态/推送目标/课表/通知开关/推送失败计数，支持探针测试）。
- 推送失败计数与阈值提示；拉取失败指数退避。
- Session 凭据 Fernet 加密存储（含旧明文自动迁移）、滚动续期自动写回、过期自动检测并提醒重新登录。
- 多用户独立绑定，推送目标结构化（FriendMessage 私聊推送，平台按 name 自愈）。
